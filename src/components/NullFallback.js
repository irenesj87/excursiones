/**
 * Componente que no renderiza nada. Es útil como fallback para Suspense
 * cuando se quiere que el componente hijo gestione su propio estado de carga.
 * @returns {null}
 */
const NullFallback = () => null;

export default NullFallback;